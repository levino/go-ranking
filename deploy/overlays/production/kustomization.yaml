apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: go-liga
resources:
  - ../../base
  - sealed-secret.yaml
images:
  - name: ghcr.io/levino/go-ranking
    # Flux ImageUpdateAutomation rewrites this tag from ImagePolicy
    # `flux-system:go-liga` (the newest ^\d{14}$ build timestamp). CI only
    # builds + pushes; it never writes to the cluster or this file.
    newTag: "5a85c0b54fd63ba293d13b1d085bb1d4577c579d" # {"$imagepolicy": "flux-system:go-liga:tag"}
