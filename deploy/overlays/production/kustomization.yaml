apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: go-liga
resources:
- ../../base
- sealed-secret.yaml
  # The app's own deploy workflow runs `kustomize edit set image` here and
  # commits the bump back to main; Flux (Kustomization go-liga) reconciles
  # it onto the cluster. No CI->cluster connection, no image-automation CRDs.
images:
- name: ghcr.io/levino/go-ranking
  newName: ghcr.io/levino/go-ranking
  newTag: 94eb702335905923219e8051d7e0741c9c13a737
